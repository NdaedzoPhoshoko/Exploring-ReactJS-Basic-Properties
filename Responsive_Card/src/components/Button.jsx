/*
Component is a piece of UI that has its own logic and appearance, and can be reused
A component can be a function, a button, an entire page, or a section of a page
*/

function Button() {
    return (
        <button>Click Me</button>
    );
}

export default Button;